# EHR-blockchain プロジェクト指示

## プロジェクトの目的

`source/emr_blockchain_conclusion_verified.md`（電子カルテ×ブロックチェーンの最終結論整理）を題材に、次の2つを達成する。

1. **情報検証** — 文書の主張（外部事実 + 過去会話由来の設計結論）が正しいかを調査・裏付け/反証する。
2. **可視化** — 検証ステータスを埋め込んだ、人間に分かりやすいインタラクティブ HTML を作る。

## ディレクトリ構成

| ディレクトリ | 役割 | 編集方針 |
|---|---|---|
| `source/` | 原典 md（不変の入力） | 原則として変更しない。参照専用 |
| `verification/` | 検証成果物。research 最終レポートのコピー、検証マトリクス md | 検証フェーズで生成 |
| `site/` | HTML 成果物。`site/assets/` に CSS/JS/画像 | HTML 開発フェーズで生成 |
| `planning/` | 計画・進捗ドキュメント | 随時更新 |

## 確定方針（2026-05-25 ユーザー合意）

1. **検証範囲**: 外部事実3点（厚労省ガイドライン第6.0版・FHIR・HSM 定義）＋ 結論1〜11の技術的妥当性・最新動向（2024–2026 の研究/規制）まで網羅検証する。
2. **成果物の形**: 各主張に「検証済/未検証/要注意」等のステータスバッジと出典リンクを埋め込んだ、**単一の自己完結インタラクティブ HTML**（オフラインで開ける形）。
3. **検証の実行方法**: `research:controller-search` を `session:spawn` で**別 tmux セッションに分離して並行実行**する。本セッションでは並行して HTML 土台を設計する。

## スキル使用方針（MUST: 必要なときに必ず invoke する）

| タイミング / トリガー | invoke するスキル | 目的・備考 |
|---|---|---|
| 検証フェーズ開始 | **`session:spawn`** | 検証作業を別 tmux セッションへ起動。プロンプトは自己完結的に（コンテキスト引き継ぎなし・要約50行のみ）。`WITH_WATCH` で完了監視 |
| 別セッション内（検証本体） | **`research:controller-search`** | サブエージェント並列調査で構造化レポート生成。出力は `/tmp/research-search-*/02-final-report.md` |
| HTML 設計・実装フェーズ | **`feature-dev:feature-dev`** | 7フェーズの機能開発。**Phase 5 実装はユーザー承認必須**。Discovery→探索→質問→設計→実装→レビュー→要約 |
| git リポジトリ内ファイル編集後 | **`git`（skill）** | Conventional Commits・日本語メッセージでコミット |

## 検証の原則

- 原典が明示する**情報源区分を尊重する**: 「過去会話の記録」＝設計結論（外部未検証）、「外部資料」＝検証済み事実。両者を混同しない。
- 信頼度ラベルを区別する: `verified`（一次資料/実行で確認）/ `deduced`（型・公式 docs から）/ `inferred`（推測）/ `uncertain`。
- 検証は「裏付け」だけでなく「反証・最新化（古い記述の指摘）」も対象とする。

## 既知の制約

- 原典に `[cite:10]`〜`[cite:32]` のマーカーがあるが、**参照文献リスト本体は文書に含まれていない**。引用の完全トレースは不可。事実ベースの間接検証に留める。
- `research:controller-search` の出力は `/tmp/` の一時領域。**最終レポートを `verification/` にコピーして永続化**すること。
- `session:spawn` は **tmux 内でのみ**動作。`--worktree` は git リポジトリ必須（本プロジェクトは git 化済み）。

## git 運用

- ローカル git のみ（リモート未設定）。push はリモート設定後。
- ファイル編集後は `git` スキル経由でコミット（ユーザーが「コミットしないで」と言った場合・編集未完了時を除く）。
